# Public Redirect Repo

This public repository exists only to redirect `shakedewan.github.io` traffic to `shakedewan.com`.

The actual site source now lives in the private repository:

- `git@github.com:ShakeDewan/shake-dewan-personal-site.git`

GitHub Pages on this repo should stay configured to serve from `main` so the redirect remains active.
